config.suffixes = ['.c']

import shutil

config.riscv64_linux_gnu_gcc = shutil.which('riscv64-unknown-linux-gnu-gcc') is not None

if config.riscv64_linux_gnu_gcc:
    config.available_features.add('riscv64-unknown-linux-gnu-gcc')
